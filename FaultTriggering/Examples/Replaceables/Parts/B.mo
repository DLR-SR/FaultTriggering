within FaultTriggering.Examples.Replaceables.Parts;
model B
  extends partialModel;
  FaultOutput.ConstantBooleanFault constantBooleanFault
    annotation (Placement(transformation(extent={{-10,-10},{10,10}})));
end B;
